import { Platform } from 'react-native';

export const PHONE_NUMBER = '0000000000'; // Replace with your phone number with DDD

export const IOS_DEVICE = Platform.OS === 'ios';
export const ANDROID_DEVICE = Platform.OS === 'android';
